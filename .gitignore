# Secrets — never commit real credentials
config.php

# Database dumps (contain full data incl. password hashes)
backups/*.sql

# User-uploaded content (org docs, permit requirements, reports)
uploads/
models/uploads/

# OS / editor cruft
.DS_Store
Thumbs.db
